{
  "name": "sobrecito-scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cross-check": "ts-node --transpile-only cross_check_poseidon.ts",
    "spike-browser": "ts-node --transpile-only spike_browser_prove.ts",
    "verify-layout": "ts-node --transpile-only verify_public_inputs_layout.ts"
  },
  "dependencies": {
    "@aztec/bb.js": "0.87.0",
    "@noir-lang/noir_js": "1.0.0-beta.9",
    "@stellar/stellar-sdk": "16.0.0",
    "argparse": "^2.0.1"
  },
  "devDependencies": {
    "@types/argparse": "^2.0.16",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
